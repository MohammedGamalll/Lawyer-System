# electron-updater feed (optional generic provider)

Default publish is GitHub Releases for MohammedGamalll/Lawyer-System.
Leave settings.update_feed_url empty unless you host latest.yml yourself.

After `npm run dist`, electron-builder writes latest.yml next to the NSIS installer.
After `npm run dist:publish`, that file is attached to the GitHub Release.
